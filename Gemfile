source "https://rubygems.org"

# This will help ensure the proper Jekyll version is running.
gem "jekyll", "~> 4.1.0"

# fix of a potential security vulnerability
gem "kramdown", ">= 2.3.0"

group :jekyll_plugins do
  gem 'jekyll-paginate'
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
end